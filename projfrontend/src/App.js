// Actually we don't need App.js totally. So I am throwing only some basic stuffs. I don't even export it
import React from "react"

export default function Routes(){
    return(
        <div>
          Just For fun. I don't prefer blank statements. That's why filling it.
        </div>
    )
}